[
    %% SASL config
    {sasl, [
            {sasl_error_logger, {file, "{{sasl_error_log}}"}},
            {errlog_type, error},
            {error_logger_mf_dir, "{{sasl_log_dir}}"},      % Log directory
            {error_logger_mf_maxbytes, 10485760},   % 10 MB max file size
            {error_logger_mf_maxfiles, 5}           % 5 files max
        ]},

    {sc_push, [
            {services, [
                    [
                        {name, 'nullsvc'},
                        {mod, 'sc_push_svc_null'},
                        {description, "Null Push Service"},
                        {sessions, [
                                [
                                    {name, 'nullsvc-com.example.NullService'},
                                    {mod, sc_push_svc_null_srv},
                                    {config, []}
                                ]
                            ]}
                    ]
                ]}
        ]},

     {apns_erl, [
             {service, [
                     {name, apns},
                     {mod, sc_push_svc_apns},
                     {description, "APNS Push Service"}
                 ]},

             {sessions, [
%%                   [
%%                       {name, 'apns-com.example.ST2'},
%%                       {config, [
%%                               {host, "gateway.sandbox.push.apple.com"},
%%                               {port, 2195},
%%                               {bundle_seed_id, <<"com.example.SomeApp">>},
%%                               {bundle_id, <<"com.example.SomeApp">>},
%%                               % "Fake" token to be used in production:
%%                               {fake_token, <<"deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef">>},
%%                               {ssl_opts, [
%%                                       {certfile, "{{platform_etc_dir}}/certs/com.example.SomeApp--DEV.cert.pem"},
%%                                       {keyfile, "{{platform_etc_dir}}/certs/com.example.SomeApp--DEV.key.unencrypted.pem"},
%%                                       {versions, ['tlsv1.1']} % Workaround for SSL issue http://erlang.org/pipermail/erlang-questions/2015-June/084935.html
%%                                   ]}
%%                           ]}
%%                   ]
                 ]}
         ]},

     {gcm_erl, [
             {service, [
                     {name, gcm},
                     {mod, sc_push_svc_gcm},
                     {description, "Google Cloud Messaging Service"}
                 ]},

             {sessions, [
%%                   [
%%                       {name, 'gcm-com.example.SomeApp'},
%%                       {config, [
%%                               {api_key, <<"***************************************">>},
%%                               {ssl_opts, [{verify, verify_none}]},
%%                               {uri, "https://gcm-http.googleapis.com/gcm/send"},
%%                               {max_attempts, 5},
%%                               {retry_interval, 1},
%%                               {max_req_ttl, 600}
%%                           ]}
%%                   ]
                 ]}
         ]},

    {lager, [
            %% What handlers to install with what arguments
            {handlers, [
                    {lager_console_backend, info},
                    {lager_file_backend, [{file, "{{platform_log_dir}}/error.log"}, {level, error}, {size, 10485760}, {date, "$D0"}, {count, 5}]},
                    {lager_file_backend, [{file, "{{platform_log_dir}}/console.log"}, {level, info}, {size, 10485760}, {date, "$D0"}, {count, 5}]}
                ]},
            %% Whether to write a crash log, and where. Undefined means no crash logger.
            {crash_log, "{{platform_log_dir}}/crash.log"},
            %% Maximum size in bytes of events in the crash log - defaults to 65536
            {crash_log_msg_size, 65536},
            %% Maximum size of the crash log in bytes, before its rotated, set
            %% to 0 to disable rotation - default is 0
            {crash_log_size, 10485760},
            %% What time to rotate the crash log - default is no time
            %% rotation. See the README for a description of this format.
            {crash_log_date, "$D0"},
            %% Number of rotated crash logs to keep, 0 means keep only the
            %% current one - default is 0
            {crash_log_count, 5},
            %% Whether to redirect error_logger messages into lager - defaults to true
            {error_logger_redirect, true}
        ]}
].


